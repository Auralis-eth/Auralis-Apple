// Copy this file to Secrets.local.xcconfig and keep the real file out of version control.
// The app reads keys from Info.plist, and the plist values are injected from these build settings.

AURALIS_ALCHEMY_API_KEY =
